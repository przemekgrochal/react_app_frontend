import React from 'react';

const HomePageButton = () => {

    const handleButton = (e) => {
            window.location="/";
    }

    return (
        <>
            {/*<button type="button" className="btn btn-light" onClick={handleButton}>Przejdź do strony głównej</button>*/}
        </>
    );
}

export default HomePageButton;